services:
  - type: web
    name: telegram-bingo
    runtime: node
    buildCommand: echo 'No build step required'
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
